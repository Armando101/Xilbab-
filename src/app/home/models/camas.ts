export class Camas {

	public name_country: String;
	constructor(
		// public _id: string,
		public code: string,
		public bedsTotal: number 
	) {
		// this.name_country = this.codes.find(element => element[1]==code_country)[0];
	}
}