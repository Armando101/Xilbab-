import { CodeCountryPipe } from './code-country.pipe';

describe('CodeCountryPipe', () => {
  it('create an instance', () => {
    const pipe = new CodeCountryPipe();
    expect(pipe).toBeTruthy();
  });
});
